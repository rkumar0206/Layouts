<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.layouts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".manageTheRenters.MTRAddRenterActivity"></activity>
        <activity android:name=".manageTheRenters.MTRShowBillActivity" />
        <activity android:name=".manageTheRenters.MTR_RenterActivity" />
        <activity android:name=".manageTheRenters.MTRAddPaymentActivity" />
        <activity android:name=".manageTheRenters.MTRProfileActivity" />
        <activity android:name=".manageTheRenters.MTRHomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>